<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function demo_ctools_plugins_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'ctools') {
    return 'plugins/' . $plugin_type;
  }
}
